import 'package:flutter/material.dart';

class BiometricWeb extends StatelessWidget {
  const BiometricWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Biometric Usability Testing - Web')),
      body: const Center(child: Text('Biometric Usability Testing for Web')), 
    );
  }
}
