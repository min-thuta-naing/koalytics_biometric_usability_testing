import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";

const SUSQuesionPreview = () => {
    const { formId } = useParams();

    return (
        <div>
            <h1>hey</h1>
        </div>
    )
};

export default SUSQuesionPreview;